import React from 'react';
import RegisterForm from '../../pages-section/register';

export const metadata = {
    title: 'Register form',
    description: 'Log app register form page',
    keywords: 'Register form ',
};

const RegisterPage = () => <RegisterForm />;

export default RegisterPage;
